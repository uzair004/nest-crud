import { HttpExceptionFilterTsFilter } from './http-exception-filter.ts.filter';

describe('HttpExceptionFilterTsFilter', () => {
  it('should be defined', () => {
    expect(new HttpExceptionFilterTsFilter()).toBeDefined();
  });
});
